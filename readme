# Panda Robot ROS 2 Description & Simulation

这是一个基于 ROS 2 Jazzy 的 Franka Emika Panda 机械臂仿真项目。

## 功能
- 完整的 URDF 模型，支持 Gazebo 物理仿真。
- 集成了 `ros2_control` 轨迹控制器。
- 包含基于 `ikpy` 的实时逆运动学（IK）控制脚本。

## 环境要求
- Ubuntu 24.04
- ROS 2 Jazzy
- Python 库: `pip install ikpy numpy`

## 如何运行
1. 启动仿真环境：
   `ros2 launch panda_description panda_launch.py`
2. 运行逆解控制脚本：
   `python3 scripts/panda_ik_teleop.py`